# Hot-Reloadable Hook Configuration
#
# Changes to this file take effect IMMEDIATELY without session restart.
# The dispatcher.sh reads this file on every hook invocation.
#
# Structure:
#   hooks:
#     <HookType>:
#       - matcher: <regex pattern for tool name, optional>
#         commands:
#           - <command to run>
#         timeout: <seconds, default 30>
#
# Available hook types:
#   - PreToolUse: Before a tool is executed
#   - PostToolUse: After a tool completes
#   - Stop: Before response is shown to user
#   - UserPromptSubmit: When user submits a message
#   - SubagentStop: When a subagent completes
#
# Commands receive JSON input on stdin and can output:
#   - {"decision": "block", "reason": "..."} to block
#   - Text to inject into context
#   - Nothing to pass through

hooks:
  # Pre-tool validation
  PreToolUse:
    - matcher: "Bash"
      commands:
        - "$HOME/.claude/hooks/enforce-user-directives.sh"
        - "$HOME/.claude/hooks/block-session-killers.sh"
        # V17 Phase 7: verify-infra-target runs BEFORE block-destructive so
        # the target-aware gate (Phase 3) gets first say, and Phase 4's
        # shared-service-yank category acts as a fall-through defense in
        # depth rather than the only thing fired.
        - "$HOME/.claude/hooks/verify-infra-target.sh"
        - "$HOME/.claude/hooks/block-destructive.sh"
        - "$HOME/.claude/hooks/block-secrets.sh"
        - "$HOME/.claude/hooks/unknown-tool-detector.sh"
        - "$HOME/.claude/hooks/enforce-tool-rules.sh"
        - "$HOME/.claude/hooks/warn-network-egress.sh"
        - "$HOME/.claude/hooks/enforce-claude-md.sh"
        - "$HOME/.claude/hooks/filter-verbose-output.sh"
      timeout: 10
    - matcher: "Read"
      commands:
        - "$HOME/.claude/hooks/block-secrets.sh"
        - "$HOME/.claude/hooks/read-tracker.sh"
      timeout: 5
    # Track MCP tool calls (for RAG source validation)
    - matcher: "mcp__"
      commands:
        - "$HOME/.claude/hooks/verify-infra-target.sh"
      timeout: 5
    # FORCE RAG search before any substantive action this turn.
    # Replaces the Stop-hook RAG_REQUIRED check (which wasted tokens by
    # blocking a fully-generated response). Blocks BEFORE the tool runs,
    # cost = a single tool-call round trip, not a whole response.
    - matcher: "Edit|Write|Bash|Task|NotebookEdit|WebFetch|WebSearch"
      commands:
        - "$HOME/.claude/hooks/require-rag-pretooluse.sh"
      timeout: 5
    - matcher: "Glob"
      commands:
        - "$HOME/.claude/hooks/enforce-claude-md.sh"
      timeout: 5
    - matcher: "Grep"
      commands:
        - "$HOME/.claude/hooks/enforce-claude-md.sh"
      timeout: 5
    - matcher: "Task"
      commands:
        - "$HOME/.claude/hooks/enforce-task-routing.sh"
        - "$HOME/.claude/hooks/agent-resource-limits.sh"
        - "$HOME/.claude/hooks/agent-tracker.sh"
      timeout: 10
    # SHORTCUT BLOCKER - Prevents bypassing existing code paths
    # Created 2026-03-18 after duplicate order incident
    # SENSITIVE PATH RESTRICTION - Blocks writes to ~/.ssh, /etc, .env, etc.
    # USER DIRECTIVE ENFORCEMENT - HARD BLOCK when user said "don't modify"
    # GITLAB CI REGISTRY - Blocks unnecessary registry config (GitLab handles it)
    # INTENT ENFORCEMENT - Blocks modifications when intent="conversation"
    - matcher: "Edit|Write"
      commands:
        - "$HOME/.claude/hooks/enforce-intent.sh"
        - "$HOME/.claude/hooks/enforce-user-directives.sh"
        - "$HOME/.claude/hooks/block-shortcuts.sh"
        - "$HOME/.claude/hooks/restrict-sensitive-paths.sh"
        - "$HOME/.claude/hooks/block-gitlab-registry-config.sh"
      timeout: 10
    # LEARNING CITATION VALIDATION - Requires verified sources for log_learning
    - matcher: "mcp__rag__log_learning"
      commands:
        - "$HOME/.claude/hooks/validate-learning-source.sh"
      timeout: 5
    # DATA SOVEREIGNTY - Block writes whose company arg belongs to a different
    # surface than the target MCP server (multi-instance RAG; registry-driven)
    - matcher: "mcp__.*__log_(decision|learning)"
      commands:
        - "$HOME/.claude/hooks/enforce-surface-routing.sh"
      timeout: 5
  # Response validation (rule enforcement) + context extraction + session hygiene + QUALITY ENFORCEMENT
  # Context extraction only runs when context usage > 70% (checked via transcript file size)
  # Quality enforcer BLOCKS half-assed work, deferrals, and missing RAG logging
  # Block sync runs AFTER quality enforcer to capture any new blocks and persist to RAG
  # QUESTION ANSWERING ENFORCEMENT - Blocks deflection, requires direct answers
  # WORKFLOW CHECKPOINT - Auto-saves progress every 50 tool calls or 30 minutes
  # ESCALATION TRACKER - V11: Detects repeated blocks, warns when model isn't self-correcting
  # RAG GIVEUP ENFORCEMENT - Blocks "can't/not available" unless RAG was searched first
  Stop:
    - commands:
        - "$HOME/.claude/hooks/block-slur-deflection.sh"
        - "$HOME/.claude/hooks/enforce-rag-post-compaction.sh"
        - "$HOME/.claude/hooks/enforce-rag-before-giveup.sh"
        - "$HOME/.claude/hooks/enforce-question-answering.sh"
        - "$HOME/.claude/hooks/quality-enforcer.sh"
        - "$HOME/.claude/hooks/escalation-tracker.sh"
        - "$HOME/.claude/hooks/pre-completion-verification.sh"
        - "$HOME/.claude/hooks/workflow-checkpoint.sh"
      timeout: 15
  # Pre-compaction extraction - runs BEFORE compaction to save context
  # This is more reliable than Stop hooks which may not fire after compaction
  PreCompact:
    - commands:
        - "$HOME/.claude/hooks/pre-compaction-extractor.sh"
      timeout: 120
  # Context injection on user prompt + RAG reminders
  # USER DIRECTIVE TRACKING - Records "don't modify" instructions for enforcement
  # BUDGET ENFORCEMENT - Warns/blocks when session costs approach limits
  UserPromptSubmit:
    - commands:
        - "$HOME/.claude/hooks/budget-enforcer.sh"
        - "$HOME/.claude/hooks/track-user-directives.sh"
        - "$HOME/.claude/hooks/track-user-prompts.sh"
        - "$HOME/.claude/hooks/pane-status-update.sh"
        - "$HOME/.claude/hooks/model-router.sh"
        - "$HOME/.claude/hooks/auto-project-context.sh"
        - "$HOME/.claude/hooks/rag-context-reminder.sh"
        - "$HOME/.claude/hooks/agent-triggers.sh"
        - "$HOME/.claude/hooks/workflow-detector.sh"
        - "$HOME/.claude/hooks/multi-session-context.sh"
        # Type 2 coordination: surface relevant peer-stream activity via MCP (advisory)
        - "$HOME/.claude/hooks/coord-stream-inject.sh"
      timeout: 10
  # Notification on subagent completion
  SubagentStop:
    - commands:
        - "$HOME/.claude/hooks/agent-tracker.sh"
        - "$HOME/.claude/hooks/task-completed-notify.sh"
      timeout: 5
  # Post-tool actions
  PostToolUse:
    - matcher: "Edit|Write"
      commands:
        - "$HOME/.claude/hooks/post-code-review.sh"
        - "$HOME/.claude/hooks/weekly-update-reminder.sh"
      timeout: 15
    - matcher: "Bash"
      commands:
        - "$HOME/.claude/hooks/sanitize-output.sh"
        - "$HOME/.claude/hooks/weekly-update-reminder.sh"
        - "$HOME/.claude/hooks/session-source-tracker.sh"
        # Type 2 coordination: capture git commits into the peer-stream feed via MCP (advisory)
        - "$HOME/.claude/hooks/coord-stream-capture.sh"
      timeout: 10
    - matcher: "Read"
      commands:
        - "$HOME/.claude/hooks/session-source-tracker.sh"
      timeout: 5
    # Track RAG logging to verify memory is being used
    - matcher: "mcp__rag__log"
      commands:
        - "$HOME/.claude/hooks/rag-usage-tracker.sh"
      timeout: 5
    # Track ANY RAG call to clear post-compaction enforcement
    - matcher: "mcp__rag__"
      commands:
        - "$HOME/.claude/hooks/rag-call-tracker.sh"
      timeout: 5
    # Type 2 coordination: mirror explicit decisions into the peer-stream feed via MCP (advisory)
    - matcher: "mcp__rag__log_decision"
      commands:
        - "$HOME/.claude/hooks/coord-stream-decision.sh"
      timeout: 8
  # Session start hooks (new session or after compaction)
  # CRITICAL: validate-hooks.sh MUST run first to fix permissions before other hooks
  # CRITICAL: session-health-check.sh verifies all systems are available
  # CRITICAL: session-memory-loader.sh loads RAG context so Claude has memory
  SessionStart:
    - matcher: "new"
      commands:
        - "$HOME/.claude/hooks/validate-hooks.sh"
        - "$HOME/.claude/hooks/session-health-check.sh"
        - "$HOME/.claude/hooks/session-memory-loader.sh"
        - "$HOME/.claude/hooks/enforce-claude-md-reading.sh"
        - "$HOME/.claude/hooks/weekly-update-init.sh"
        - "$HOME/.claude/hooks/coord-session-register.sh"
      timeout: 30
    - matcher: "compact"
      commands:
        - "$HOME/.claude/hooks/validate-hooks.sh"
        - "$HOME/.claude/hooks/session-health-check.sh"
        - "$HOME/.claude/hooks/session-memory-loader.sh"
        - "$HOME/.claude/hooks/compaction-state-tracker.sh"
        - "$HOME/.claude/hooks/post-compaction-reminder.sh"
        - "$HOME/.claude/hooks/weekly-update-init.sh"
        - "$HOME/.claude/hooks/coord-session-register.sh"
      timeout: 30
    - matcher: "resume"
      commands:
        - "$HOME/.claude/hooks/session-health-check.sh"
        - "$HOME/.claude/hooks/session-memory-loader.sh"
        - "$HOME/.claude/hooks/coord-session-register.sh"
      timeout: 30
# Settings
settings:
  # Enable debug logging
  debug: false
  # Default timeout for hooks without explicit timeout
  default_timeout: 30
